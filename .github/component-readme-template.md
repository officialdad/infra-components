<!--
Canonical component README. Copy to <component>/README.md and fill it in. House style (see CLAUDE.md
"Component README style"): lead with the noun, active voice, one idea per bullet, defaults in
`backticks`, footguns in a `> ⚠️` blockquote — not marketing prose. The Inputs/Outputs tables are
GENERATED by terraform-docs between the markers below — never hand-edit them. Exemplars: vpc, ec2.
Required sections (enforced by scripts/check-readme-structure.sh): What it creates, Auth,
Dependencies, and the TF_DOCS markers.
-->
# <component>

One-or-more `<thing>` on **`<CLOUD>`** — <the single defining opinion, e.g. no public IP, SSM-only>.

## What it creates

- **`<Resource>`** — <what it is + the key default in `backticks`>.
- **`<Resource>`** — <concise clause>.

> ⚠️ **<footgun, if any>** — <one or two lines on the sharp edge and how to avoid it>.

<!-- Optional narrative subsections (Access model, Bootstrap, Exposing a service, …) go here — only
     where a component genuinely needs them. Keep them bulleted and concise. -->

## Auth

Provider needs `<CLOUD>` credentials (env vars / ADC / CI role) — supplied out-of-band, none stored
here. Region/project comes from `var.global.deploy_region`.

## Dependencies

<!-- Bullets mapping upstream component outputs → this component's inputs. Write "None." for a
     foundation component that consumes nothing. -->
- Consumes `<output>` from the **`<upstream>`** component → `<input>`.

<!-- For a map/object input (e.g. instances, repositories), add a short prose "entry shape"
     explainer here, before the generated tables. The generated Inputs table is the authoritative
     reference; this paragraph explains the per-field intent that the collapsed type can't. -->

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
