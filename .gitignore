# Terraform / Terragrunt artifacts — never commit these
.terraform/
.terraform.lock.hcl
.terragrunt-cache/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# Local overrides
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Editor / OS
.idea/
.vscode/
.DS_Store

# Claude Code — commit team config (settings.json, hooks/, skills) but not personal overrides
.claude/settings.local.json
